# NeMo Sortformer diarization backend.
#
# Рекомендуемая установка в venv:
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt -r requirements-cuda12.txt -r requirements-nemo.txt
#
# Для Linux нужны системные аудио-библиотеки и ffmpeg:
#   sudo apt install ffmpeg libsndfile1
#
Cython
packaging
nemo_toolkit[asr]
