# Дополнительно при ошибке «libcublas.so.12 is not found» на Linux + NVIDIA.
#
# На Ubuntu/Debian системный Python «externally managed» — ставить так:
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt -r requirements-cuda12.txt
#
# Или в user-site (без venv):
#   python3 -m pip install --user --break-system-packages -r requirements-cuda12.txt
#
nvidia-cublas-cu12>=12.1.0
